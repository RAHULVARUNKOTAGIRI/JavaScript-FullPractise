console.log("welcome");
alert("welcome to the alert command chapter");
window.alert("welcome to the alert command chapter");

//we can also use alert without window as it is the global object in browser environment

// Here window is the global object and alert is a method of the window object. So we can call it directly without using window.

console.log(window);
// In the above code, we are logging the window object to the console. The window object represents the browser window and provides various properties and methods to interact with it.


console.log(this);

//windows==this: botha are global variables
